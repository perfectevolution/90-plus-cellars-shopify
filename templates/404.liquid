<div class="container py-5 my-5">
  <div class="row">
    <div class="col text-center">    
      <h1 class="mb-2">{{ 'templates.404.title' | t }}</h1>
      <h2 class = 'mb-4'>{{ 'templates.404.subtext' | t }}</h2>
      <p>
        <a href="{{ routes.root_url }}" class='btn btn-primary' >Keep Shopping</a>
      </p>
    </div>
  </div>
</div>
